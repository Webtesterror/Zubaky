'use client';
import {useState} from 'react';
export default function Setup(){const [error,setError]=useState(''),[busy,setBusy]=useState(false);return <main className="login"><h1>Aktivace správce</h1><p>Zadejte jednorázový kód z předaného návodu. Správa webu se propojí s vaším přihlášeným účtem.</p><form className="form-grid" onSubmit={async e=>{e.preventDefault();setBusy(true);try{const code=new FormData(e.currentTarget).get('code');const r=await fetch('/api/setup',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({code})});const j=await r.json() as {error?:string};if(!r.ok)throw Error(j.error);location.reload()}catch(e){setError(e instanceof Error?e.message:'Aktivace se nezdařila.')}finally{setBusy(false)}}}><label className="field">Aktivační kód<input type="password" name="code" required autoComplete="off"/></label><button className="primary" disabled={busy}>Aktivovat správu</button>{error&&<p role="alert">{error}</p>}</form><p><a href="/">Zpět na web</a></p></main>}
