import {env} from 'cloudflare:workers';
export async function GET(_req:Request,{params}:{params:Promise<{key:string}>}){try{const {key}=await params;if(!/^[a-f0-9-]+\.(jpg|png|webp)$/.test(key))return new Response('Nenalezeno',{status:404});const object=await env.BUCKET?.get(key);if(!object)return new Response('Nenalezeno',{status:404});return new Response(object.body,{headers:{'Content-Type':object.httpMetadata?.contentType||'application/octet-stream','Cache-Control':'public,max-age=31536000,immutable','X-Content-Type-Options':'nosniff'}})}catch{return new Response('Fotografie není dostupná',{status:503})}}
